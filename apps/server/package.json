{
  "name": "agent-city-server",
  "private": true,
  "version": "0.1.0",
  "description": "Agent City Fastify API and local agent runtime.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/*.test.js",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "overrides": {
    "fast-uri": "3.1.3"
  },
  "dependencies": {
    "@fastify/static": "^10.1.0",
    "fastify": "^5.10.0",
    "mammoth": "^1.12.0",
    "pdf-parse": "^2.4.5",
    "read-excel-file": "^9.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
